name: Electron 企业级构建与发布流水线（Windows）

permissions:
  contents: write

on:
  push:
    branches:
      - master

jobs:
  build:
    name: 构建 Windows 安装包
    runs-on: windows-latest

    outputs:
      version: ${{ steps.get_version.outputs.version }}
      build_time: ${{ steps.get_time.outputs.time }}

    steps:
      - name: 检出代码仓库
        uses: actions/checkout@v4

      - name: 安装 Node.js 环境
        uses: actions/setup-node@v4
        with:
          node-version: "24"
          cache: "npm"

      - name: 安装项目依赖
        run: npm install

      - name: 读取项目版本号
        id: get_version
        run: |
          $version = node -p "require('./package.json').version"
          echo "version=$version" >> $env:GITHUB_OUTPUT

      - name: 获取构建时间
        id: get_time
        run: |
          $time = Get-Date -Format "yyyy-MM-dd HH:mm:ss"
          echo "time=$time" >> $env:GITHUB_OUTPUT

      - name: 构建 Windows 应用安装包
        run: npm run build:win

      - name: 上传 MSI 构建产物
        uses: actions/upload-artifact@v4
        with:
          name: windows-msi
          path: |
            dist/**/*.msi

  release:
    name: 创建或更新 Release
    needs: build
    runs-on: ubuntu-latest

    steps:
      - name: 检出代码仓库
        uses: actions/checkout@v4

      - name: 下载构建产物
        uses: actions/download-artifact@v4
        with:
          name: windows-msi
          path: dist/

      - name: 删除同名 Tag（如果存在）
        run: |
          git fetch --tags
          if git rev-parse "v${{ needs.build.outputs.version }}" >/dev/null 2>&1; then
            git tag -d v${{ needs.build.outputs.version }}
            git push origin :refs/tags/v${{ needs.build.outputs.version }}
          fi

      - name: 创建新 Tag
        run: |
          git tag v${{ needs.build.outputs.version }}
          git push origin v${{ needs.build.outputs.version }}

      - name: 创建或覆盖 Release（仅 MSI）
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ needs.build.outputs.version }}
          name: v${{ needs.build.outputs.version }}
          body: |
            🚀 版本：v${{ needs.build.outputs.version }}

            ⏱ 构建时间：${{ needs.build.outputs.build_time }}

          files: |
            dist/**/*.msi
          draft: false
          prerelease: false
          fail_on_unmatched_files: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
